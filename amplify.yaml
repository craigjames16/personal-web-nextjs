version: 1
app:
  environment:
    OPENAI_API_KEY: $OPENAI_API_KEY
    GOOGLE_TOKEN: $GOOGLE_TOKEN
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
      - ../public/**/*
  cache:
    paths:
      - .next/cache/**/*
      - .npm/**/*
      - node_modules/**/*